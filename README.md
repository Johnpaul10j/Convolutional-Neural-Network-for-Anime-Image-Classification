# Convolutional-Neural-Network-for-Anime-Image-Classification
Trained a Convolutional Neural Network (CNN) for image classification. The goal is to train a model that can classify images into the correct categories.
